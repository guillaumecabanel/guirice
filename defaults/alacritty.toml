[env]
TERM = "xterm-256color"

[shell]
program = "zellij"
args = ["a", "Office"]

[window]
dimensions = { columns = 180, lines = 54 }
padding.x = 0
padding.y = 0
decorations = "None"
opacity = 0.97


[keyboard]
bindings = [
{ key = "F11", action = "ToggleFullscreen" }
]
